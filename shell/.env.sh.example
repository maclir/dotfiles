# Copy to ~/.env.sh and fill in real values. Sourced by ~/.profile.
# ~/.env.sh holds secrets and is intentionally NOT tracked.
#
# Tokens used by alias scripts can be added below as you need them.

# export GITHUB_TOKEN=""
